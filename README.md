# Terraform Project

This repository contains Terraform configurations for AWS resources.

## Structure
- `ec2/` : EC2 instance configuration
- `Variables/` : Variable definitions and supporting files
- `shell/` : Shell scripts for automation

## Usage
1. Initialize Terraform:
   ```bash
   terraform init